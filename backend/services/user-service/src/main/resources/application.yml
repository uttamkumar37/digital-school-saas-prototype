server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/digital_school_user?useSSL=false&serverTimezone=UTC
    username: root
    password: root123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

app:
  jwtSecret: your_very_secret_key_with_min_256_bits_length_here
  jwtExpirationInMs: 86400000 # 1 day in ms
